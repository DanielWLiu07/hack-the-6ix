# Master → Workers broadcast

- [17 Jul] Kickoff. Follow status/ASSIGNMENTS.md. Report after every milestone. No git commands — master owns git. Schemas in root CLAUDE.md are law.
- [22:12] @deploy: VITE_SERVER_URL prod value is DEFERRED by design — it will be the laptop's hotspot IP, unknowable until venue setup. Document a `vercel env add VITE_SERVER_URL production` + redeploy step in DEPLOY.md, use `http://localhost:3001` as the placeholder, mark the item done, and move on.
- [22:12] @fw-mcu @fw-linux: firmware/BRIDGE.md (MCU↔Linux RPC contract) is now published by fw-tools. Read it and conform your interfaces to it exactly; flag conflicts via BLOCKED status entries, not by editing BRIDGE.md.
- [22:25] LIDAR CLARIFICATION (from human): TWO lidar systems. (1) RPLIDAR **C1** 2D 360° on the robot → SLAM/occupancy via `lidar_scan` events. (2) **iPhone lidar** handheld scan → colored 3D world reconstruction. @lidar-pi: you now also own `robot/lidar/phone/` — see your updated ASSIGNMENTS section (task 4: Polycam/Scaniverse GLB → optimize → `web/public/world.glb` + conventions doc + sample GLB). @web-frontend: plan to render `world.glb` as the colored 3D environment in the lidar view with the live C1 scan overlaid — lidar-pi will post conventions.
- [22:25] @lidar-sim: continue emitting C1-style scans; no change.
- [22:32] PHASE 2 for completed workers: @server-core → Base44 webhook forwarder (docs/BASE44.md "Integration" — env-gated BASE44_WEBHOOK_URL + secret header on every pick_event) + wire llm-client's service.py nl_command path. @server-test → extend suite to cover both once landed + robot-client integration test. @lidar-pi → your ASSIGNMENTS section gained tasks 4–5 (iPhone lidar → world.glb pipeline) — start task 4. @vision-infer → UNO Q deployment prep (ARM packaging) + draft docs/QUALCOMM.md with MPU/MCU split + bench FPS. @lidar-sim → task-3 stretch + scripted room-tour for demo video. @llm-client → end-to-end nl_command demo transcript (10 commands through hub → farmhand → robot mock). @llm-data → bonus preference-pair (chosen/rejected) dataset for RL, document for Freesolo judges.
- [phase 3] @llm-client → draft docs/DEVPOST.md sections for the Freesolo track (dataset stats, eval accuracy, demo transcript excerpts) + a shot list for the FarmHand demo video segment. @vision-infer → draft docs/DEVPOST.md sections for Qualcomm (MPU/MCU split + on-device FPS from QUALCOMM.md) and Deloitte AI-for-Green (quantified impact: kg waste avoided/hr, 5W edge inference vs cloud). Coordinate: llm-client creates the file skeleton first, vision-infer appends their sections.
- [phase 4 — demo readiness] @fw-tools → scripts/demo.sh: ONE command that boots the whole demo (hub, robot node in mock, lidar sim, dev server, :8123 scene) + docs/INTEGRATION.md first-power-on hardware runbook (bench-test order, smoke tests, rollback). @llm-client → 3-minute pitch script with timings + speaker roles (env hook → live pick → dashboard → MPU/MCU slide → FarmHand), file docs/PITCH.md. @llm-data → judge-facing README.md overhaul (you now own README.md): architecture mermaid, quickstart via demo.sh, track-by-track evidence links. @server-core → demo panic switch: env/endpoint to force-sim mid-demo if the real robot dies during judging, documented in docs/DEPLOY.md. @db (when free) → docs/IMPACT.md: defensible methodology for waste-avoided/ROI numbers with sources (Deloitte + env track ammo). @vision-train → post a training progress/ETA status now.
- [hardware update from human] ALL 3D PRINTS ARE DONE (arm parts, gripper, fruit props, bins). @vision-train: create ml/ripeness/raw/ intake with a README telling humans exactly how to photograph the printed props (angles, lighting, count) for the real-data fine-tune; be ready to run the 30-min relabel+finetune loop the moment images land. @fw-linux @fw-mcu: assembly starts soon — have the pose recorder + bench-test mode ready for first power-on per docs/INTEGRATION.md (fw-tools is writing it).
- [self-containment] Local scene folder renamed: web/legacy-ui → web/orchard-scene (server still :8123, already restarted). @web-frontend: update any "legacy-ui" references/comments to "orchard-scene", AND make the DEV landing degrade gracefully when :8123 is unreachable (fresh cloners won't have the untracked folder — probe, then fall back to the r3f/classic hero instead of a dead iframe). @fw-tools: demo.sh — update the path and guard the scene step with a directory-exists check so demo.sh runs clean on a fresh clone. @vision-train: when exporting final v0, strip ONNX metadata/doc_string (current exports embed local absolute paths — leaks the machine username into a judged artifact).
